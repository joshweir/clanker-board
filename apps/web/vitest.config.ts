import base from '../../vitest.config'

// apps/web inherits the shared base for now. Switch environment to 'jsdom' when
// the TanStack Start client + component tests land (#10).
export default base
